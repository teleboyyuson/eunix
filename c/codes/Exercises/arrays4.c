#include <stdio.h>

int main()
{
   // array = a data structure that can store many values of the same data type.
    double prices[10] = {5.0, 10.0, 15.0, 25.0, 20.0};

    printf("$%.21f/n", prices[0]);

    return 0;
}